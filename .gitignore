
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Git worktrees (superpowers skill)
.worktrees/

# Sensitive: account registry with plaintext passwords — NEVER commit
docs/Реестр аккаунтов.xlsx
docs/*.xlsx
